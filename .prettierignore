# Build and dependency output
.next
node_modules
*.tsbuildinfo

# Generated: rewriting these serves nothing and risks the install
pnpm-lock.yaml
test-results

# Other agents' git worktrees are separate checkouts of this same repo —
# formatting them from here rewrites files a different session is editing.
.claude

# Generated by drizzle-kit. Load-bearing, not tidiness: prettier has no SQL
# parser, so `--check` errors on the migration outright, and reformatting one
# would change the sha256 the migrator records for it. Drizzle rewrites the
# meta files on the next generate anyway.
drizzle
