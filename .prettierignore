# Build and dependency output
.next
node_modules
*.tsbuildinfo

# Generated: rewriting these serves nothing and risks the install
pnpm-lock.yaml
test-results

# Other agents' git worktrees are separate checkouts of this same repo —
# formatting them from here rewrites files a different session is editing.
.claude

# Generated by drizzle-kit. Needed: without it `format:check` fails on both
# files under drizzle/meta, which drizzle rewrites on the next `db:generate`,
# so prettier and drizzle would reformat them at each other forever. The .sql
# plays no part — a directory scan silently skips a file prettier has no parser
# for, and prettier only errors when such a file is named explicitly.
drizzle
